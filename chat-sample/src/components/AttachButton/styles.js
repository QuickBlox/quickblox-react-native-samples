import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  attachButton: {
    alignItems: 'center',
    justifyContent: 'center',
    padding: 6,
  },
  attachButtonImage: {
    height: 30,
    resizeMode: 'contain',
    width: 30,
  },
})